use flake .

if [ -f .env ]; then
  dotenv .env
fi

# Python setup via uv
layout_python_uv() {
  if [ ! -d $PWD/.venv ]; then
    uv venv .venv --python 3.11
  fi
  source .venv/bin/activate
}

export VIRTUAL_ENV=$PWD/.venv
layout_python_uv

