apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: { { include "meteo.fullname" . } }
spec:
  gatewayClassName: { { include "meteo.fullname" . } }
  listeners:
    - name: http
      protocol: HTTP
      port: 80
---
